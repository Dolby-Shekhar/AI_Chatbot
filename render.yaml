services:
  - type: web
    name: ai-chatbot-node
    runtime: node
    region: oregon
    buildCommand: npm install
    startCommand: bash start.sh
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: PORT
        value: "10000"
